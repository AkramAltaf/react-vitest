import { Button } from "@mui/material";
import styles from "./button.module.scss";

const FSButton = () => {
  return (
    <Button variant="contained" className={styles.FSbutton}>
      Styled Button
    </Button>
  );
};

export default FSButton;
